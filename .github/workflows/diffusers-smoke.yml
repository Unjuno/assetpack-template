name: diffusers-smoke

on:
  workflow_dispatch:
    inputs:
      model:
        description: Model candidate to test
        required: true
        default: tiny-sd
        type: choice
        options:
          - tiny-sd
          - sd-turbo
          - bk-sdm-tiny
          - all

permissions:
  actions: read
  contents: read

concurrency:
  group: diffusers-smoke-queue
  cancel-in-progress: false

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Observe workflow queue
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh run list --workflow diffusers-smoke.yml --status queued --limit 20 || true
          gh run list --workflow diffusers-smoke.yml --status in_progress --limit 20 || true

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyyaml pillow torch diffusers transformers accelerate safetensors

      - name: Run CPU smoke test
        run: |
          python scripts/run_diffusers_smoke.py --model "${{ inputs.model }}" --out reports/diffusers-smoke

      - name: Upload smoke report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: diffusers-smoke-${{ inputs.model }}
          path: reports/diffusers-smoke/
          retention-days: 7
