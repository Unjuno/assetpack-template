name: bonsai-lowbit-smoke

on:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: bonsai-lowbit-smoke-queue
  cancel-in-progress: false

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      HF_HUB_DISABLE_TELEMETRY: "1"
      PIP_DISABLE_PIP_VERSION_CHECK: "1"
      PYTHONUNBUFFERED: "1"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install probe dependencies
        run: |
          python --version
          df -h
          free -h
          nproc
          python -m pip install --upgrade pip
          pip install --no-cache-dir torch --index-url https://download.pytorch.org/whl/cpu
          pip install --no-cache-dir pyyaml huggingface_hub safetensors onnx onnxscript onnxruntime
          python -c "import torch, huggingface_hub, yaml, safetensors, onnx, onnxscript, onnxruntime as ort; print('torch', torch.__version__); print('cuda_available', torch.cuda.is_available()); print('huggingface_hub', huggingface_hub.__version__); print('onnx', onnx.__version__); print('onnxscript', onnxscript.__version__); print('onnxruntime', ort.__version__)"

      - name: Run low-bit smoke probes
        run: |
          set -euo pipefail
          scripts=(
            "scripts/run_bonsai_lowbit_probe.py --self-test"
            "scripts/run_bonsai_lowbit_probe.py"
            "scripts/probe_bonsai_lowbit_layer_cols.py"
            "scripts/compare_bonsai_lowbit_to_unpacked_layer.py"
            "scripts/compare_bonsai_lowbit_to_unpacked_layers.py"
            "scripts/compare_bonsai_lowbit_to_unpacked_all_layers.py"
            "scripts/export_bonsai_lowbit_layer_onnx.py"
            "scripts/export_bonsai_lowbit_runtime_linear_onnx.py"
            "scripts/export_bonsai_lowbit_multi_linear_onnx.py"
            "scripts/export_bonsai_lowbit_same_block_projection_onnx.py"
            "scripts/export_bonsai_lowbit_qkv_mlp_split_onnx.py"
            "scripts/export_bonsai_lowbit_qkv_head_reshape_onnx.py"
            "scripts/export_bonsai_lowbit_qkv_seq_head_layout_onnx.py"
            "scripts/export_bonsai_lowbit_attention_math_onnx.py"
            "scripts/export_bonsai_lowbit_attention_context_composition_onnx.py"
            "scripts/export_bonsai_lowbit_attention_to_out_input_source_onnx.py"
            "scripts/export_bonsai_lowbit_attention_to_out_semantic_input_onnx.py"
            "scripts/export_bonsai_lowbit_attention_to_out_onnx.py"
            "scripts/export_bonsai_lowbit_single_block_modulated_onnx.py"
            "scripts/export_bonsai_lowbit_two_single_blocks_modulated_onnx.py"
            "scripts/validate_bonsai_lowbit_artifacts.py"
            "scripts/validate_bonsai_lowbit_attention_context_composition.py"
            "scripts/validate_bonsai_lowbit_attention_to_out_input_source.py"
            "scripts/validate_bonsai_lowbit_attention_to_out_semantic_input.py"
            "scripts/validate_bonsai_lowbit_attention_to_out.py"
            "scripts/validate_bonsai_lowbit_single_block_modulated.py"
            "scripts/validate_bonsai_lowbit_two_single_blocks_modulated.py"
          )
          for s in "${scripts[@]}"; do
            echo "::group::python $s"
            python $s
            echo "::endgroup::"
          done

      - name: Show low-bit report summary
        if: always()
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          reports = sorted(Path('reports').glob('**/report.json'))
          for p in reports:
            try:
              r = json.loads(p.read_text())
            except Exception as exc:
              print(json.dumps({'report': str(p), 'error': str(exc)}, indent=2))
              continue
            print(json.dumps({
              'report': str(p),
              'ok': r.get('ok'),
              'graph_kind': r.get('graph_kind'),
              'block_kind': r.get('block_kind'),
              'bundle_kind': r.get('bundle_kind'),
              'layer': r.get('layer'),
              'block_index': r.get('block_index'),
              'block_indices': r.get('block_indices'),
              'sequence_block_count': r.get('sequence_block_count'),
              'hidden_dim': r.get('hidden_dim'),
              'projection_output_dim': r.get('projection_output_dim'),
              'sequence_length': r.get('sequence_length'),
              'head_schema': r.get('head_schema'),
              'composition_schema': r.get('composition_schema'),
              'source_candidate_schema': r.get('source_candidate_schema'),
              'semantic_input_schema': r.get('semantic_input_schema'),
              'to_out_input_schema': r.get('to_out_input_schema'),
              'to_out_schema': r.get('to_out_schema'),
              'modulation_schema': r.get('modulation_schema'),
              'residual_schema': r.get('residual_schema'),
              'lowbit_layers': r.get('lowbit_layers'),
              'split_schema': r.get('split_schema'),
              'onnx_size_bytes': r.get('onnx_size_bytes'),
              'external_data_size_bytes': r.get('external_data_size_bytes'),
              'max_abs_error': r.get('max_abs_error'),
              'allclose': r.get('allclose_rtol_1e_4_atol_1e_5'),
              'all_outputs_allclose': r.get('all_outputs_allclose_rtol_1e_4_atol_1e_5'),
              'claim': r.get('claim'),
            }, indent=2))
          PY

      - name: Upload low-bit reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bonsai-lowbit-smoke-report
          path: reports/
          retention-days: 7
