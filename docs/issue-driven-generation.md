# Issue-driven workflow

GitHub Issues provide structured requests.

```text
Issue -> label gate -> validation -> fixed recipe -> assets/generated -> Issue comment
```

Validation rejects missing fields, non-ASCII fields, invalid licenses, invalid models, URLs, unknown sections, missing required terms, and duplicate recipe IDs.
