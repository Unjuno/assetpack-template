# CI records

This directory keeps only template-relevant CI records.

Current selected image models are documented in:

```text
docs/ci/image-model-final-selection.md
docs/ci/image-model-final-selection.json
```

Issue-driven generation commits accepted assets under:

```text
assets/generated/
```

Old model-search and benchmark records are not part of the active template surface.
