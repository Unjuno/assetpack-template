# Image storage

Issue-generated PNG files are uploaded as GitHub Actions artifacts.

They are not committed to Git by default.

Current retention:

```text
7 days
```

Keep the repository small. Store configuration, recipes, reports, and docs in Git. Use a separate review workflow for any permanent image publishing.
